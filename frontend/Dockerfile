# Use a lightweight Nginx image
FROM nginx:alpine

# Copy the static content from the frontend directory
COPY . /usr/share/nginx/html

# Expose port 80 for the web server
EXPOSE 80

# The default Nginx command will be used to start the server
